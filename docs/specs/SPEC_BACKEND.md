# Backend (Web Server) Spec - pokemon-locations

> **This document has been superseded.**
>
> The web server spec now lives in [`SPEC_WEBSERVER.md`](SPEC_WEBSERVER.md). It covers the same component (the ASP.NET BFF that sits between the browser and the Pokémon Locations API) and is kept up to date with the current implementation.
>
> This file remains as a redirect for any existing references; new edits should target `SPEC_WEBSERVER.md`.
